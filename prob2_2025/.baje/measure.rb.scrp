Instead of saying the "smallest <M>\sigma</M>-algebra containing the intervals" we more often say "<M>\sigma</M>-algebra
 generated by the intervals
-------EOD-------
by <M>\lambda(B).</M>
-------EOD-------
