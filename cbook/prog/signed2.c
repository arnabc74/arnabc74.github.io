#include <stdio.h>

int main() {
  int x, y;
  unsigned int a, b;

  printf("Supply an unsigned integer: "); 
  scanf("%u",&a); 
  printf("a = %d (using %%d)\n",a);`\includegraphics{arrow.pdf}`
  printf("a = %u (using %%u)\n",a);`\includegraphics{arrow.pdf}`

  return 0;
}
