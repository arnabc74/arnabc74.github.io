@{<NOTE>
<HEAD1>Type of interpolation</HEAD1>

By default the interpolation is done using spline. This is often
not desirable, as the behaviour on one side of the keyframe may
affect the behaviour on the other side. Then we should use linear
interpolation. 

</NOTE>@}
